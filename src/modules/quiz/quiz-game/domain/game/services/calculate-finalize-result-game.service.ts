import { Game } from '../game.entity';
import { Player } from '../../player/player.entity';


export const calculateFinalizeResultGame = async (
  game: Game,
  firstPlayer: Player,
  secondPlayer: Player,
) =>{





}